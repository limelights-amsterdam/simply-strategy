# Plan 2026

A stand-in for the client document the worked example cites. The example is
anonymised, so the sources it points at are anonymised too. It exists so that
`check_artifact.py examples/ --material examples/material/` can prove that every
pointer on the page names a file that is actually there.
